<div class="comments">
  {{#each this as |comment|}}
    <section class="comment">
      <div class="meta">
        {{comment.user.username}} on {{format_date comment.created_at}}
      </div>
      <div class="text">
        {{comment.comment_text}}
      </div>
    </section>
  {{/each}}
</div>
